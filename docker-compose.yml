version: '3.8'

services:
  streamlit-app:
    build: .
    container_name: movies-rag-app
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
      - ./.env:/app/.env
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
